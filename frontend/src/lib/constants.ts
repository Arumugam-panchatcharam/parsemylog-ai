/** Default TanStack Query stale time (matches prior App.tsx behavior). */
export const QUERY_STALE_TIME_MS = 30_000;

export const QUERY_RETRY_DEFAULT = 1;

/** MAC OUI batch lookup limit (server-aligned). */
export const MAC_LOOKUP_MAX_COUNT = 100;

/** Debounce / UX timings */
export const OUI_SUCCESS_TOAST_MS = 5_000;
